# Evolution ledger

One line per incident. Appended by the `evolve` skill. If this file is empty after a month of building, the system is not learning and something upstream is wrong.

Format: `YYYY-MM-DD · <what went wrong> · <layer fixed> · <change made>`

---

2026-09-06 · New-user signup (Google and email) failed: `/app` rendered the inline error state instead of onboarding. The hosted project was missing migrations `0010`–`0015` (the visible failure was `profiles.degree_text` from `0015`, but `migration list` showed every migration since `0009` unapplied), so `getProfile`'s full `SELECT` errored — the second occurrence of the exact `0009` schema-drift shape (hosted DB behind the committed migrations; `pnpm db:reset` only resets the **local** stack). Same signature both times: auth/signup succeeds, then the profile read fails, and the user is stuck before onboarding · operational · root cause of *why it keeps happening*: the project was **unlinked**, so nothing connected the committed migrations to the hosted DB, and `supabase db push` has no local record of hosted. Fixed by `supabase link --project-ref uukkbuusviipgfzmgnzs` (resolves the correct pooler host — `aws-1-eu-west-2.pooler.supabase.com`, reachable over IPv4 where `db.<ref>.supabase.co` is IPv6-only and unreachable) then `supabase db push`, which applied `0010`–`0015`. Defensively — because this is now a *repeat* incident, not a first one — added `scripts/check-migration-drift.mjs`, wired as `predev`: when `.env.local` targets a NON-local project it compares local `supabase/migrations/*.sql` against the hosted `supabase_migrations.schema_migrations` and blocks `pnpm dev` listing the missing migrations. It fails closed (a hosted target whose migration state cannot be read — no `SUPABASE_DB_URL`, wrong-format URL, unreachable DB — is exactly the state that shipped drift twice), with `SKIP_MIGRATION_DRIFT_CHECK=1` as the explicit offline escape. `SUPABASE_DB_URL` must be a **Postgres** connection string (`postgresql://…`), not the HTTP API URL — the first integration of the guard tripped over exactly that. `scripts/check-migration-drift.test.ts` covers the non-DB contract. Lesson: the first drift incident was treated as one-off operational fallout; the fix for the *second* is a mechanism, not a memory — and the guard only earns its keep once the project is actually linked and the DB URL is the pooler string, which is why linking is now part of the fix rather than optional.



2026-08-04 · Phase 00. The app threw "Your project's URL and Key are required" and the error pointed at the Supabase dashboard, which is the wrong advice for a local stack · code · `lib/supabase/{client,middleware}.ts` now fail with the actual fix ("create .env.local from `pnpm exec supabase status`"); `playwright.config.ts` reads the keys from the CLI so the e2e suite needs no env file at all.

2026-08-04 · Phase 00. `tailwind-merge` treated `text-ui-base` (size) and `text-on-accent` (colour) as one class group and silently dropped the colour, so the primary button rendered chalk-on-blue at 2.15:1 · code + tests · `lib/cn.ts` now declares our scales via `extendTailwindMerge`, with `lib/cn.test.ts` covering it. **Only the axe run caught this** — the button still looked deliberate, so review would not have. Evidence that §9's mechanical checks carry more weight than the written rules.

2026-08-04 · Phase 00. `claim_profile_slug` returned a free slug and the caller inserted it separately, leaving a TOCTOU window; two parallel Playwright workers hit it on the first full run and one signup died on a duplicate key · schema · replaced with `create_profile()`, which retries the INSERT itself so the unique index *is* the check. `tests/db/create-profile.test.ts` fires ten simultaneous signups with the same name. Note the shape: a bug that only appears under load, only for new users, on the one action they cannot retry.

2026-08-04 · Phase 00. The app header put three stacked-label controls in 48px of chrome; at 390px "Language" was clipped and "Sign out" wrapped onto two lines · code · collapsed them into `components/settings-menu.tsx` behind one 44px trigger. Caught by reading the screenshots, not by any assertion — the layout tests passed because nothing overflowed the *page*, only the header. Worth remembering that "no horizontal scroll" is a weaker check than looking.

2026-08-04 · Phase 00. `--text-tertiary` failed 4.5:1 in both themes at its specified value · design system · lightness adjusted (dark 0.560→0.635, light 0.600→0.515), recorded in `design-system.md` §1. Added `pnpm lint:contrast`.

2026-08-04 · Phase 00. `lint:contrast` used a hand-picked pair list and missed `--text-tertiary` on `--bg-inset` (the input placeholder); axe found it only once the a11y suite was extended to `/onboarding` · tests · the checker now cross-products every foreground against every surface, which immediately surfaced two more failures nothing rendered yet (`--warning` on `--bg-inset`, `--text-tertiary` on `--bg-overlay`). **A checker whose coverage depends on remembering combinations is not a mechanism.** Same lesson: the a11y and focus suites originally covered only public pages, and extending them to the two authed screens found both of these.

2026-08-04 · Phase 00 close. **`NEXT_PUBLIC_SUPABASE_ANON_KEY` in `.env.local` held the `sb_secret_` key**, which bypasses RLS — confirmed by SHA-256 against `supabase status`, not by eyeballing the prefix · config + tests · swapped to the publishable key; `scripts/assert-build-safe.mjs` now fails the build if secret-shaped material reaches `.next/static`. Two things make this worse than a typo: any `pnpm build` would have inlined a service-role key into a public JS chunk, and *locally the app was running with RLS entirely inert* while `test:rls` went green — because the suite reads its keys from `supabase status` and the app reads `.env.local`. **The tests certified a boundary the running app was not enforcing.** Also hardened `.claude/hooks/secret-guard.sh`, whose rule matched the variable *name* (`NEXT_PUBLIC_*SECRET*`) and so sailed past a legitimate name with a secret value.

2026-08-04 · Phase 00 close. `/api/test-auth` shipped in the production route manifest, and its second gate was a lie: `NEXT_PUBLIC_SUPABASE_URL` is inlined by Next at build time, so "refuse unless Supabase is localhost" froze into a literal describing the *build machine*. Meanwhile `lib/supabase/server.ts` reads the same variable through a dynamic `process.env[name]` lookup, which is **not** inlined — so the client talked to production while the gate still asserted localhost. A build made locally and deployed with production env was a live backdoor · code + tests · renamed to `route.e2e.ts` and gated through `pageExtensions`, so it is absent from the manifest rather than guarded at runtime; verified in both directions. The general lesson: **`NEXT_PUBLIC_*` is a build-time constant, so it can never be a runtime security check** — and a security control that lives only in a comment's confidence ("gate 2 is the one that matters") is worth exactly nothing until something executes it.

2026-08-04 · Phase 00 close. Four RLS escalations, all reproduced independently before being accepted: any user could self-grant `is_pro`, insert their own `profiles` row directly (bypassing `create_profile`, choosing any slug — permanently, because the slug-immutability trigger then locks the squat in), forge `status:'accepted'` on their own `institution_requests` row, and call `claim_profile_slug` **anonymously** as a user-existence oracle · schema (PENDING APPROVAL) · root cause is one line: `grant select, insert, update on profiles to authenticated` is table-level, so the policies authorise the *row* and nothing constrains the *columns*. The anon RPC is a separate trap — Postgres grants `EXECUTE` to `PUBLIC` by default, so the explicit `grant ... to authenticated` in the migration is decorative. `tests/rls/foundation.test.ts` tests the row dimension thoroughly and the column dimension not at all, which is precisely where all four live: **33 green tests over four working escalations.**

2026-08-04 · Phase 00 close. The first version of `assert-build-safe.mjs` — written to catch the secret-key bug above — matched a hand-guessed base64 substring for the service-role JWT claim. The alignment it guessed never occurs, so it would have reported clean forever · tests · JWT payloads are now decoded rather than pattern-matched, and `scripts/assert-build-safe.test.ts` proves every rule fires, including the false-positive case (`@supabase/supabase-js` ships a bare `startsWith("sb_secret_")` in every bundle, so a prefix-only rule would fire on every build, be dismissed as noise, and be gone by the day it mattered). Caught within an hour of writing the rule that today's other lesson was about. Worth stating flatly: **I wrote an untested check while fixing a bug caused by an untested check.**

2026-08-04 · Phase 00 close. The design-critic calibration planted a hardcoded hex, a 13px gap and a 300ms transition — and revealed that **`lint:tokens` caught 1 of the 3**, not 3. `checkCssPixels`/`checkCssDuration` ran on `.css` files only, so `style={{ gap: '13px' }}` in a `.tsx` was unguarded, while the script's own header claimed inline styles were covered · tests + code · both checks now run on every file type; `scripts/__fixtures__/violations.tsx` carries each violation in *both* syntaxes plus one reached through a variable. Two compounding causes worth naming: `off-grid-px` and `raw-duration` were the only rules missing from `expectedRules`, and they were the only rules that were silently CSS-only — **a rule with no test is a rule that can stop matching without anyone noticing**. And the fixture's header asserted a guarantee the linter did not provide, which is the false-confidence failure `docs/MEASUREMENT.md` was written to watch for. The lesson generalises past this bug: calibrating the checker found a hole in the guarantee it was being measured against. Do the same for `lint:i18n`, `lint:contrast` and the RLS suite before trusting them.

2026-08-06 · After switching to hosted Supabase, Google sign-in looped forever between `/app` and `/onboarding` ("localhost redirected you too many times"). The hosted DB was missing migration `0009` (`profiles.keyboard_shortcuts_enabled`), so `getProfile`'s full `SELECT` errored → returned null while `hasProfile` (selects only `id`) still saw the row → the two redirect sites disagreed and bounced infinitely · schema + code + tests · root cause was operational (`pnpm db:reset` resets the **local** stack, never hosted — the project was unlinked), fixed by `supabase db push --db-url "<hosted>"`; defensively, `getProfile` now returns `Result<Profile | null>` so a failed read is distinct from "no row", and `lib/app-entry.ts` (`resolveAppEntry`) maps the error arm to an inline error state instead of onboarding — the exact arm that looped. (A redirect to /login was rejected for the same reason: the middleware bounces a signed-in /login visitor straight back to /app, so that too would loop.) The design flaw generalises: two different existence checks driving two redirects can disagree under any schema drift or RLS error, and here a disagreement became an infinite redirect (no logs, browser error page) instead of an error. Same shape as the "green suite over an open hole" incidents — nothing exercised the two-checks-disagree case.

2026-08-04 · Phase 00 close. design-critic calibration result: caught the hex (by scripting a pixel read and converting OKLCH→sRGB itself), missed the 13px gap, correctly did *not* report the 300ms transition, zero false positives · AI layer · `.claude/agents/design-critic.md` now requires per-finding provenance (pixel-verified / visual / inferred), a mandatory "what I did not check" section, and forbids asserting an absence a still image cannot show. 4px-grid conformance was removed from its remit and left to `lint:tokens`. The critic behaved honestly **only because the dispatch prompt asked it to** — a quality that depends on the caller remembering to ask is not a property of the system, so it moved into the agent definition. Full write-up in `docs/MEASUREMENT.md`.

2026-08-04 · Phase 00 close. `components/ui/button.tsx` set `onClick={loading ? undefined : props.onClick}` immediately *before* `{...props}`, so the spread put the handler straight back and the loading guard was dead code · code + tests · `onClick` is destructured and applied after the spread. It never surfaced because `aria-disabled:pointer-events-none` was doing the work the JS claimed to do. `components/ui/button.test.tsx` uses `fireEvent`, not `userEvent`, precisely because userEvent honours pointer-events and would pass whether or not the guard works — the same passing-for-the-wrong-reason shape as the superuser RLS check and the un-emulated reduced-motion test below.

2026-08-04 · Phase 00 close. A code-review finding asking for `bg-color`/`border-color` overrides in `lib/cn.ts` was **rejected on evidence**: eleven adversarial cases (later-colour-wins, colour-plus-non-colour, side-specific borders, the real secondary-button class list) all pass unchanged, because unlike `text-*` those namespaces have no size scale to collide with · tests · cases kept in `lib/cn.test.ts` as regression cover for the reasoning, so if a future token is ever named `cover` or `solid` one of them fails and the override becomes justified. Worth repeating: a review finding by analogy needs a failing test before it earns a code change.

2026-08-04 · Phase 00 close. `.gitignore` used `.env*.local`, which requires the filename to *end* in `.local` — so a `.env.local.back` holding real hosted Supabase credentials was untracked-but-not-ignored, one `git add -A` from being committed · config · pattern widened to `.env` + `.env.*` with an `!.env.example` exception. Also unanchored `node_modules/`, since `/node_modules` matched the root only and let pnpm's `packages/contracts/node_modules` symlinks get committed.

2026-08-04 · Phase 00. The reduced-motion test used `test.use({ reducedMotion })`, which did not take effect — it graded un-emulated CSS and would have passed for the wrong reason just as easily as it failed · tests · switched to `page.emulateMedia()` and added an assertion that the media query actually matched. Same class of error as the first RLS check, which ran as superuser and would have reported PASS forever.

2026-08-04 · Phase 00 ship gate. **`supabase/migrations/0002_rls_column_grants.sql` was untracked** — the RLS fixes existed in the working tree but had never been committed. Production would still have table-level grants allowing any user to `UPDATE profiles SET is_pro = true` and anonymous callers to enumerate users via `claim_profile_slug()`. The tests passed because `supabase db reset` applies all migrations locally, but the migration had never been committed or applied to any real database · schema · migration staged for commit.

2026-08-04 · Phase 00 ship gate. `httpOnly: false` left refresh tokens readable via `document.cookie` for 400 days — acknowledged in a comment but not mitigated. An XSS anywhere (e.g. Tiptap in phase 02) would compromise all accounts with no rotation path · code · set `httpOnly: true` in both `lib/supabase/server.ts` and `lib/supabase/middleware.ts`. The browser client does not need `document.cookie` access — it uses automatic HTTP cookie handling.

2026-08-04 · Phase 00 ship gate. `assert-build-safe.mjs` was tacked onto `pnpm build`, so Vercel auto-builds running `next build` would skip the secret-key check entirely · config · added `vercel.json` with explicit `buildCommand: "pnpm build"` to ensure the check runs on all platform builds.

2026-08-04 · Post-ship. The settings dialog opened at roughly −100%/−100% and snapped to centre when the animation ended — the panel was visibly wrong for ~200ms · code + tests · root cause: Tailwind v4 compiles `-translate-x/y-1/2` to the `translate` shorthand, while `@keyframes dialog-enter` animated `transform: translate(-50%,-50%)`. Those are separate CSS properties that compose, so during the animation the total offset was double. Fixed by animating `scale` alone in `styles/globals.css` and leaving centring to the `translate` property. Added `e2e/design-system-authed.spec.ts` that asserts the dialog's position **mid-animation**, not only after it settles — a test that only checks the final position passes against this bug.

2026-08-04 · Post-ship. Choosing Catalan from the language picker hard-crashed the site and locked the user out; a Catalan-configured browser (`Accept-Language: ca`) crashed on first visit with no interaction at all · code + tests · `LOCALES` in the frozen contracts package contains `'ca'` (a valid profile value), but `messages/ca.json` does not exist, so the next-intl request config's dynamic import threw. Fixed by adding `lib/i18n/shipped.ts` with `SHIPPED_LOCALES = ['en','es']` and gating three entry points: the picker options, the resolver (so `Accept-Language: ca-ES-valencia` → `'en'`), and the write action. No contracts edit, no migration, no test churn. Added `lib/i18n/shipped.test.ts` that reads `messages/` and fails if the two lists diverge.

2026-08-06 · Phase 01 review. Three silent failures shipped under green tests because the suite never exercised the paths: (1) an owner DELETE of a deck/card cascaded into every subscriber's and forker's `card_states`/`review_logs` (`on delete cascade` from 0004); (2) `apply_review` set `seen_version = excluded.seen_version` on every write, so a review auto-dismissed the "this changed" flag with no dismissal recorded (ADR-002 §8); (3) `fork_course` copied notes without `published_at`, so a fork of a public course was a course full of invisible drafts — unreadable by anyone but the forker, with mutable slugs. Verification was structurally blind: no test exercised a DELETE cascade, fork output was asserted only through service_role (RLS bypassed), and AC14 called `acknowledge_card_change` twice instead of reviewing. · schema + tests · `0008_progress_and_fork_fixes.sql` adds a pair of `security definer` BEFORE DELETE triggers (deck-level primary, card-level for direct deletes) that BLOCK a delete when a `card_states` row belongs to a user other than the deck owner — the first attempt, an orphan-preserving trigger writing an `orphaned_at` tombstone, failed because `ON DELETE CASCADE` fires after the BEFORE trigger, so the rows it marked were still cascade-deleted, and an AFTER-trigger re-insert hit the FK to the now-deleted card; blocking the delete is the only legal way to protect the rows, and the abandoned `orphaned_at` column was dropped from the migration rather than shipped as a dead nullable on a hot table. Also keeps `seen_version` on the ON CONFLICT path, carries `published_at` across forks, and cancels a parent-course subscription on `fork_deck`. Added `tests/db/cascade.test.ts` and real assertions in `sharing`/`card-version` that read fork output as `anon` and exercise the review path. Note the shape: each bug was invisible to the *other* suite's assumptions (RLS bypass, cascade untested) — the fix is the test, the migration just makes it pass. Deferred: the `enforce_private_requires_pro` trigger has no WHEN clause (re-checks on every update, and fails open when the subquery returns no row), and `cards.deck_id`/`card_states.deck_id` are not coupled by a composite FK.

2026-08-06 · Phase 01 review. **Supabase's default ACL for `public` tables grants TRUNCATE (and every other privilege) to `anon` and `authenticated`** — verified by probing `pg_default_acl` and creating a fresh table as `supabase_admin`: all seven privileges appeared for both roles, including TRUNCATE, so any table a phase-02 migration forgets to `revoke` ships with destructive capability. The suite stayed green because phase 01 revoked every table's default grants explicitly · verification gap · add an audit of `pg_default_acl` + `has_table_privilege` to `tests/db/grants.test.ts` so a *forgotten* revoke is caught the moment a new table exists, not as a production incident. Not a defect in shipped code — the failing privilege is absent from every live table — but the guarantee today is "every migration happens to remember the revoke," which is a hope, not a mechanism. **The third instance of the same shape (after `lint:tokens`, `lint:contrast`): an untested check that can silently stop covering its domain.** The upstream change that would stop the fourth is a convention change in CLAUDE.md + the phase spec template — new tables' grants now ship as an explicit `revoke all on <table> from anon, authenticated;` right after `create table` (the grant pattern that 0002/0007 established), plus an audit assertion that the *default* ACL for `public` is narrowed as a belt-and-suspenders second line — so the default is safe and the checker is a guard against violating it, not the only thing standing between a table and a TRUNCATE.

2026-08-06 · Phase 01 review. **`tests/db/grants.test.ts` covers the grant/revoke discipline only for tables that already exist** — it reads `information_schema.role_table_grants` over the live schema, so a *forgotten* revoke on a future phase-02 table is invisible until someone probes the privilege, the same "green suite over an open hole" failure as the DELETE-cascade bug · tests · extend the suite with a default-ACL audit (the `pg_default_acl` + fresh-table probe above) so a new table's missed revoke is caught the moment the table exists. This is the **third instance of the same shape as `lint:tokens` (2026-08-04) and `lint:contrast` (2026-08-04)**: a check that only tests what exists today and would report PASS forever over a rule it never re-examines. Each of the three was found the same way — not by the suite, but by an adversarial probe that asked "what would a fresh case do?" — and each time the missing test was the actual bug: the code that protects today's tables does not protect tomorrow's. The upstream change that would stop the fourth instance is to make "new thing inherits unsafe defaults" a first-class failure mode: audit *defaults* (default ACL, default grants) rather than per-object state, so a future convention that introduces another default-forgetting path is caught by construction, and add the default-ACL assertion to the RLS/grant suites rather than leaving it as a one-off probe.

2026-08-06 · Phase 01 commit prep. `env.local` and `env.local.back` — both holding real credentials — sat **unignored** in the working tree, one `git add -A` from being committed · config · `.gitignore` had been widened to `.env` + `.env.*` (phase 00), but those patterns require a leading dot; the no-dot forms `env.local`/`env.local.back` match nothing, so `git check-ignore` reports them unignored. Added explicit no-dot patterns (`env.local*` covers both) to the local-env-files block, with a comment tying them to the `.env.local.bak` finding so the next variant of the class is caught by the pattern rather than by luck. Found by `git status` during commit prep, not by any check — the same "untracked secrets are only caught by eyeball" shape as the phase-00 incident.

2026-08-07 · Phase 02 post-merge. Three bugs shipped under green editor e2e, all found by hand in the browser: (1) `lib/shortcuts/use-scope.ts` / `use-shortcut.ts` depended on the context object identity (`[ctx, …]`) which the provider's `useMemo` rebuilds on every `setActiveScopes` — so `useActiveScope('editor')` triggered an infinite push/pop loop → "Maximum update depth exceeded"; the hook had never been mounted in a real screen before the editor (phase 00b committed it but never used it); (2) `components/editor/note-list.tsx` only rendered the "New note" button inside the empty-state branch — once one note existed, the button vanished and no second note could be created; the dashboard's `<Button>` was also unwired (dead click); (3) `components/editor/slash-menu.tsx` lost focus when it unmounted — the filter input's removal moved focus to `<body>` so the student's next keystrokes disappeared — and the Equation item was never added despite being in the spec. All three were invisible to the suite for the same reason: the e2e tested shallow triggers (menu opens/closes, math dialog appears) without asserting the resulting document state, had no test for multi-note creation, and never mounted `useActiveScope` to exercise the shortcut provider. code + tests · fix 1: depend on the stable `useCallback(…, [])` refs (`ctx?.pushScope` / `ctx?.register`) not the ctx object; fix 2: render CreateNoteButton in both empty and non-empty states of NoteList, wire dashboard Link to `/app/notes`; fix 3: add `onInsertEquation` prop + Equation item to SlashMenu, refocus the editor via microtask after menu unmount (same pattern as MathInput cancel). Added `e2e/editor/regression.spec.ts` asserting: heading/bullet-list/equation actions actually apply to the doc (not just that the menu closes), focus is restored after selection, the editor mounts clean without pageerrors, and a second note is createable from the list. Also added `.claude/worktrees/**` to `eslint.config.mjs` ignores — the retained phase-02 worktree contained fixture files that caused 1547 lint errors on main.

2026-08-08 · Phase 03 post-merge. Eight user-visible defects found by hand in the browser, plus four more found while investigating them — all under a green `typecheck && lint && build`. The eight: `MISSING_MESSAGE` spam in the review session; `1`-`4`/`e`/`Esc` doing nothing; unreadable card typography; a card editor with no keyboard flow; no way to see answers in a list; no way to edit a card from the study view; math extensions loaded with no trigger to invoke them; no course hierarchy. Root causes were four, not eight: (1) `useShortcut` labels passed as bare strings to a root-scoped `t`, plus ~10 `t('common.close')` calls under a non-root namespace — `scripts/lint-i18n.mjs` detects hardcoded literals but **never verifies a key resolves**, so all ~20 shipped silently; (2) `lib/shortcuts/use-shortcut.ts` excluded `onPress` from its effect deps behind an eslint-disable, freezing every review handler's closure at mount with `phase='front'`, so `if (phase === 'grading')` was permanently false — no test had ever mounted the hook and dispatched a real key event; (3) `.note-doc-view` had **zero CSS anywhere** while `.tiptap` carried a complete 118-line typography block, and Tailwind preflight strips UA margins, so every card rendered as undifferentiated 14px sans; (4) `courses` was fully built in `0003` — tables, RLS, FKs, grants, Zod input — and 100% dormant, with `lib/db/decks.ts` hardcoding `course_id: null`. The four bonus defects: `submitReview` hardcoded `queue: []` so the session "completed" after every single grade (`[]` is truthy with length 0); **14 classes compile to no CSS at all** because Tailwind v4's `--container-*: initial` reset makes `max-w-3xl` a no-op rather than an error, leaving deck detail full-bleed at 1440px and the review card with no width cap; `NoteDocView` wrapped block elements in `<span>`, which the parser reparents; and `edited_during_review` was hardcoded `false` with the changed-card dialog never triggered, so phase-03 AC10/AC11/AC12 were **recorded as passing against constants** · verification gap + code + schema · phase 03b (`specs/phase-03b-remediation.md`) lands the missing checks **first and failing**, before any fix, so the blast radius is enumerated by a machine rather than by anyone's reading: `lint-i18n.mjs` gains key-existence, template-prefix, shortcut-label and locale-parity checks; a new `assert-classes-compile.mjs` fails the build on any class that emits no CSS; `use-shortcut.test.tsx` dispatches a real `window` keydown after a state change. **Note the shape: this is the fourth and fifth instance of "a check that only tests what already exists"** (after `lint:tokens`, `lint:contrast`, and the `grants.test.ts` default-ACL hole) — and both new instances are the *same* variant, a linter whose domain silently excludes the thing that broke. Two consequences worth carrying forward. First, `lint:tokens`' own header already documented that the namespace reset "makes off-token utilities produce NO CSS, it does not make them an error" — the hole was written down and still shipped, so **a documented gap is not a mitigated one**; the fix has to be a check, never a comment. Second, both `use-shortcut` tests are mandatory together: adding `onPress` to the deps array makes the stale-closure test pass and reintroduces the 2026-08-07 "Maximum update depth exceeded" loop, so without a register-called-once assertion the fix oscillates between two bugs, each of which makes the other's test pass. Each new checker ships with two-directional fixtures asserting an exact finding count, and the i18n checker must **report** call sites it could not statically resolve rather than counting them as clean — the coverage-shrink failure mode this ledger has now recorded five times.
