// Client: KaTeX stylesheet must load from a component in the allowlist
// (components/editor/**). The woff2 fonts are bundled in the katex package
// and served from /_next/static — no runtime Google Fonts request (§2).

import 'katex/dist/katex.min.css';
